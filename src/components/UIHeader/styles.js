import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    backgroundColor: '#fb6c54',
    height: 80,
    alignItems: 'center',
    paddingHorizontal: 12,
  },
  icon: {
    fontSize: 25,
    color: '#fff',
  },
  titleContainer: {
    flex: 1,
    alignItems: 'center',
  },
  title: {
    fontSize: 22,
    color: '#fff',
  },
});

export default styles;
